#include<stdio.h>

main()
{
  int a[10],n,i,sum=0;
  
  printf("How many inputs:\n");
  scanf("%d",&n);
  
  printf("Enter the numbers:\n");
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
    
   for(i=0;i<n;i++)
    sum=sum+a[i];
    
   printf("Sum of the array elements is = %d",sum);
}
